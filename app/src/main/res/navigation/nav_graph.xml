<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_login">

    <fragment
        android:id="@+id/navigation_log"
        android:name="com.zybooks.weighttracker.WeightLog"
        android:label="Weight Tracker"
        tools:layout="@layout/fragment_weight_log" />

    <fragment
        android:id="@+id/navigation_login"
        android:name="com.zybooks.weighttracker.Login"
        android:label="Welcome to Weight Tracker!"
        tools:layout="@layout/fragment_login" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.zybooks.weighttracker.Settings"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />
</navigation>